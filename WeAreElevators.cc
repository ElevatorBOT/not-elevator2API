using WeAreDevs;

  if WeAreDevs and ElevatorPlugin.net then
      WeAreDevs.Kexts.com.System.boot
  end
  
  function using(tag)
  
  using and tag==["dll"]
  
 using dll.com.windows; do [dll.mainloop]
 
              if not WeAreDevs then
                wrd2.com/download-api
             end
             
             define com.WeAreDevs:Remove(a)
             
             a:Remove
             
                  newpromise(wearedevs.dll)
                  
                          using prompt.exe;
                          
                          
                              prompt:LaunchElevatorBrowser(website: wearedevs.system/wrd2api)
                              
                              
                              return promise(Prompt.NewShell.CurrrentCommand)
                              
                              shell.import(wearedevs.dll), elevator.exe
                              
                              
               if microsoft.pc then
               return
               end
               
               if apple.pc or os then
               return apple.com
               end
               
               if wine.LinuxApp then
               end
               
 load()
